{% extends 'base.html.twig' %}

{% block body %}
    <div class="card text-center">
        {{ form_start(formUser) }}
        {{ form_row(formUser.username, {'attr': {'value': user.username }}) }}
        {{ form_row(formUser.email, {'attr': {'value': user.email }}) }}
        {{ form_row(formUser.description, {'attr': {'value': user.description }}) }}
        <br>
        <button type="submit" class="btn btn-primary" onclick="return confirm('Souhaitez-vous vraiment modifier le profil ?')">Modifier le profil</button>
        {{ form_end(formUser) }}
        <br>
    </div>
{% endblock %}