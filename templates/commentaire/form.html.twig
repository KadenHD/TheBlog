{% extends 'base.html.twig' %}

{% block body %}
    <div class="container" style="padding-top: 2%">
        <div class="row">
            <h1 class="h3 mb-3 font-weight-normal text-center">Poster un commentaire</h1>
            <div class="card text-center">
                    {{ form_start(formCommentaire) }}
                    {{ form_row(formCommentaire.contenu) }}
                    <button type="submit" class="btn btn-primary" style="margin-top: 0.5%;%" onclick="return confirm('Souhaitez-vous vraiment envoyer ce Commentaire ?')">Envoyer</button>
                    {{ form_end(formCommentaire) }}
            </div>
        </div>
    </div>
{% endblock %}
