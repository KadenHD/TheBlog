{% extends 'base.html.twig' %}

{% block title %}Email envoyé !{% endblock %}

{% block body %}

        {% include "navbar.html.twig" %}
        <main>
                <div class="container">
                        <div class="row">
                                <h1 class="h3 mb-3 font-weight-normal text-center">Email de réinitialisation de mot de passe envoyé</h1>
                                <div class="card text-center">
                                    <p>
                                        Si un compte possède cet adresse Email, alors un mail contenant le lien pour réinitialiser votre mot de passe vient d'être envoyé !
                                        Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                                    </p>
                                    <p>Si vous ne recevez pas le mail, vérifié dans vos spam ou alors <a href="{{ path('app_forgot_password_request') }}">Recommencez</a>.</p>
                                </div>
                        </div>
                </div>
        </main>
        {% include "footer.html.twig" %}
{% endblock %}